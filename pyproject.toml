[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pkgman"
version = "2.2.8"
description = "Declarative layer over OS package managers. Replay your manually installed packages on fresh machines."
requires-python = ">=3.11"
authors = [{name = "Jose"}]
license = {text = "MIT"}

[project.scripts]
pkgman = "pkgman:main"

[tool.setuptools]
py-modules = ["pkgman", "commands", "database", "managers", "constants", "output", "cli", "runner", "ui", "sys_check", "command_doctor"]

[dependency-groups]
dev = ["pytest"]